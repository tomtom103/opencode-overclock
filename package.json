{
  "name": "opencode-overclock",
  "version": "0.0.1",
  "description": "Claude Code features backported to opencode. Modular, toggleable.",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/index.ts",
    "./tui": "./src/tui.ts"
  },
  "files": [
    "src"
  ],
  "keywords": [
    "opencode",
    "opencode-plugin",
    "claude-code"
  ],
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "check": "tsc --noEmit && prettier --check ."
  },
  "dependencies": {
    "@opencode-ai/plugin": "latest",
    "croner": "^10.0.1"
  },
  "devDependencies": {
    "@opentui/core": "0.4.5",
    "@opentui/keymap": "0.4.5",
    "@opentui/solid": "0.4.5",
    "@types/bun": "latest",
    "prettier": "^3.9.6",
    "typescript": "^5.7.0"
  }
}
